COMPOSER_PROMPT = """Answer the user query using retrieved and external tool context.
Output must be clear, structured, and grounded.
Include specific files for each significant claim.
If information is missing, say so explicitly.
"""
